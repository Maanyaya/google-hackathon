# Drop your MCP setup screen recording here as:
#   mcp-setup.mp4
#
# Then rebuild the frontend:
#   cd frontend && npm run build
#
# The dashboard Setup section auto-embeds /dashboard/videos/mcp-setup.mp4
# unless MODEX_MCP_SETUP_VIDEO_URL is set on Cloud Run (YouTube/Loom/Drive).
